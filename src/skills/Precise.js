import Skill from './Skill';

const options = {
	name: 'Precise',
	numericalBonus: '',
	effect: 'Reroll 1 die or add 1 to its value'
};
// let preciseAspect = SkillAspect(name: "Precise", numericalBonus: "", effect: "Reroll 1 die or add 1 to its value")

export default new Skill(options);
