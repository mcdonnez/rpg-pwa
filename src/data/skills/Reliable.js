import Skill from './Skill';
// let reliableAspect = SkillAspect(name: "Reliable", numericalBonus: "", effect: "Reroll a 1")

const options = {
	name: 'Reliable',
	numericalBonus: '',
	effect: 'Reroll a 1'
};

export default new Skill(options);
